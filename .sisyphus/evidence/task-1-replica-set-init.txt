SCENARIO 1: Replica Set Initializes on Fresh Start

Preconditions:
- Ran: docker compose -f docker-compose.test.yml down -v
- No running containers or volumes

Steps Executed:
1. docker compose -f docker-compose.test.yml up -d
2. Waited 15 seconds for services to start
3. Ran: docker compose -f docker-compose.test.yml exec db mongosh --eval "rs.status().ok"

Result:
Output: 1

Status: ✓ PASS - Replica set initialized successfully on fresh start
- rs.status().ok returns 1 (replica set is initialized)
- Both db and init-replica services started without errors
- MongoDB listening on 0.0.0.0:27017 (via --bind_ip_all)
- Healthcheck validated MongoDB was ready before init-replica ran
