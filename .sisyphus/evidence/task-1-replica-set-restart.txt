SCENARIO 2: Replica Set Survives Restart

Preconditions:
- Replica set already initialized from Scenario 1
- Previous test output: rs.status().ok = 1

Steps Executed:
1. docker compose -f docker-compose.test.yml restart db
2. Waited 5 seconds for MongoDB to restart
3. Ran: docker compose -f docker-compose.test.yml exec db mongosh --eval "rs.status().ok"

Result:
Output: 1

Status: ✓ PASS - Replica set configuration persisted across restart
- rs.status().ok still returns 1 after db restart
- MongoDB replication coordinator maintained state
- Data volume preserved replica set configuration
